const Keys = {
    RESOLUTION: 'radar.resolution',
    RANGE: 'radar.range',
};

const defaultState = {
    [Keys.RESOLUTION]: 0,
    [Keys.RANGE]: 10
};

export default {
    Keys,
    defaultState
};
